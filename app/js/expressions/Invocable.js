import { InterpreterVisitor } from "../ast/interpreter.js";
import { Environment} from "../ast/environment.js";

export class Invocable {

    arity() {

    }

    /**
     * @param {InterpreterVisitor} interpreter
     * @param {Array<any>} args
     */
    invocable(interpreter, args) {

    }
}